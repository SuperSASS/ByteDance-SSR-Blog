{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* 纯 Node 环境，不需要 DOM */
    "lib": ["ES2020"],

    // 关键：确保这里显式指定 JSX 运行时
    "jsx": "react-jsx",

    /* 后端要产出 dist 交给 Node/pm2 等运行 */
    "noEmit": false,
    "rootDir": "src",
    "outDir": "dist",

    /* 路径别名 */
    "baseUrl": "../..",
    "paths": {
      "@/*": ["apps/web/src/*"], // 这里与 web 项目中的路径别名保持一致，不能指向自己，否则会冲突
      "@shared/*": ["packages/shared/src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "../../packages/shared" }]
}
