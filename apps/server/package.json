{
  "name": "ssr-blog-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch src --exec ts-node src/app.ts",
    "build": "tsc",
    "lint": "eslint .",
    "start": "node dist/app.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
