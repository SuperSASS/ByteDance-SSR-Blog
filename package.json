{
  "name": "ssr-blog-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A SSR & SPA blog based on React 18 + Koa 3 + TypeScript + Vite, with monorepo organization.",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter ssr-blog-web dev",
    "dev:server": "pnpm --filter ssr-blog-server dev",
    "build": "pnpm --filter ssr-blog-shared build && pnpm --filter ssr-blog-server build && pnpm --filter ssr-blog-web build",
    "pack:production": "pnpm build && node scripts/pack-production.js",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css}\"",
    "prepare": "husky install",
    "kill": "node scripts/kill-port.js 3000",
    "db:migration": "npx prisma migrate dev",
    "db:client": "npx prisma generate",
    "db:reset": "tsx scripts/seed-db.ts",
    "db:studio": "prisma studio --url file:dev.db"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@eslint/js": "^9.0.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.10.1",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "eslint-plugin-react": "^7.0.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.4.0",
    "fs-extra": "^11.3.2",
    "globals": "^16.5.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.2.7",
    "nodemon": "^3.1.11",
    "prettier": "^3.0.0",
    "prisma": "^7.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.0.0",
    "vite": "^7.2.6"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,css,md}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@prisma/client": "^7.0.1"
  }
}
