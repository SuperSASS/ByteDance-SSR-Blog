# Server
PORT=3000
NODE_ENV=development

# 基础 URL (可选)
# 开发环境通常不需要设置,会自动从请求头获取 (如 http://localhost:3000)
# 生产环境建议明确设置,例如:
# BASE_URL=https://yourdomain.com
# BASE_URL=https://blog.example.com

# Database (阶段 2 会用到)
DATABASE_URL="file:../../dev.db"

# JWT (阶段 6 会用到)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=2h
